import 'package:flutter/material.dart';

class CourseCategoryView extends StatelessWidget {
  const CourseCategoryView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}